  

<div class="mt-4">
  <div class="row">
    <div class="col-lg-8">
      <h3>
        {{course.name}}
      </h3>
      <p>{{course.description}}</p>
    </div>
    <div class="col-lg-4">
      <div class="card" style="width: 18rem;">
        <iframe src="https://www.youtube.com/embed/{{course.video}}">
        </iframe>
        <div class="card-body">
          <h5 class="card-title">{{course.name}}</h5>
          <p class="card-text">{{course.description}}</p>
          <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
      </div>
    </div>
  </div>
</div>